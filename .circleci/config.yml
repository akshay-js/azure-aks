orbs:
  docker: circleci/docker@1.5.0
version: 2.1
executors:
  docker-publisher: 
    environment:
      IMAGE_NAME: akshayasharma770/node-test-aks
    docker:
      - image: circleci/node:latest

jobs:
    buildNodejs:
      executor: docker-publisher
      steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Install dependencies
          command: npm i
    publishLatestToHub: 
      executor: docker-publisher
      steps: 
        - checkout
        - setup_remote_docker
        - run: 
            name: Publish Docker Image to Docker Hub
            command: |
              sudo docker logout
              sudo docker login -u "$DOCKER_USERNAME" -p "$DOCKERHUB_PASSWORD" docker.io --password-stdin --help
              sudo docker build -t mylocalimage -f Dockerfile .
              sudo docker tag mylocalimage akshayasharma770/testing:myfirstimagepush
              docker images
              sudo docker push akshayasharma770/mylocalimage:myfirstimagepush
workflows:
  version: 2
  build-master:
    jobs:
       - buildNodejs
       - publishLatestToHub
